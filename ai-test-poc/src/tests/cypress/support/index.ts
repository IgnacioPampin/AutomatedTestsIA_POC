import './commands';

// Global configuration for Cypress tests can be set here
Cypress.config({
  // Example configuration options
  defaultCommandTimeout: 10000,
  // Add other global settings as needed
});